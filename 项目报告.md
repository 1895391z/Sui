# AI驱动的Aspen HYSYS反应器仿真系统项目报告

## 一、项目目标

本项目面向Aspen HYSYS V15，目标是让用户通过自然语言提交化工反应工况，由系统识别受支持的反应场景、选择相应反应器模板、驱动HYSYS求解并返回结构化结果。当前实现覆盖甲苯歧化、甲烷蒸汽重整和水煤浆气化三个考核场景，同时提供命令行入口与本地浏览器聊天界面。

## 二、技术方案

系统采用Python 3.12和pywin32，通过`HYSYS.Application` COM接口连接HYSYS。整体分为五层：

1. `app`负责HTTP服务、聊天状态、国内大模型调用和前端交互；
2. `core`负责自然语言解析、严格数据契约、场景路由、执行编排和结果标准化；
3. `adapters`负责三个场景的HYSYS对象访问、参数写入、求解和结果读取；
4. `cases`保存只读seed、运行副本和SHA-256清单；
5. `tools/tests`负责证据采集、完整性检查和离线回归测试。

自然语言首先进入确定性解析器。解析器识别场景及流量、温度、压力、转化率、汽碳比和煤浆浓度，并对单位缺失、冲突数值以及未被消费的工程参数进行拦截。只有通过校验的`CaseSpec`才能进入适配器。大模型不直接生成或执行Python/COM代码，主要承担结果中文说明以及多轮对话中的“修改工况/解释结果”判断，从而降低模型幻觉直接影响仿真的风险。

## 三、HYSYS执行与可靠性

每个适配器使用经过人工建立和实机验证的HYSYS seed。运行前复制到`cases/runtime`，验证反应器类型、物流连接、Reaction Set和活动反应，再写入本轮输入。求解完成后读取流股、组成、转化率、热负荷和质量/元素衡算，保存运行副本并再次校验seed哈希。任何关键阶段失败都会返回非零状态，不会输出虚假成功。

HYSYS冷启动使用普通GUI启动后轮询ROT中的COM对象，避免直接`/Automation`冷激活导致的CLR问题。针对本机出现过的`IFace.dll 0xc0000005`启动崩溃，系统增加了失败清理和最多两次启动尝试。Web聊天中的单工况在第一次成功后保留并复用HYSYS实例，避免连续提问时反复冷启动；每个Simulation Case仍会在本轮结束时关闭。

## 四、三个场景结果

甲苯歧化采用Conversion Reactor，因为用户明确给出50%转化率且不要求动力学。10000 kg/h、380°C、25 bar工况下，未反应甲苯约5000.000 kg/h，苯约2119.311 kg/h，总二甲苯约2880.689 kg/h，质量衡算误差为0%。HYSYS模型以p-Xylene代表总二甲苯，邻/间/对分布由显式选择性假设推导，默认等比例。

甲烷蒸汽重整采用Equilibrium Reactor，因为主反应和水煤气变换均为可逆反应且未提供动力学。100 kgmol/h、S/C=2.7、520°C、13.5 bar条件下，710°C出口的甲烷转化率为54.0348%，热负荷为1080.757 kW；600°C出口的转化率为30.3523%，热负荷为544.851 kW。升温使转化率提高23.6824个百分点，同时增加约535.905 kW热负荷，趋势符合吸热重整反应预期。

水煤浆气化采用Gibbs Reactor，因为体系高温、反应复杂且需要考虑未知副产物。1000 kg/h、煤质量分数62%、40°C、40 bar、出口1400°C工况下，CO收率为40.8627%，碳转化率为61.2941%，热负荷为1487.581 kW，质量衡算误差约0.002214%。模型将煤近似为纯碳且无氧气进料。1400°C超过当前组分报告的426.85°C Gibbs数据上限，因此该结果虽然数学收敛，但工程验证状态保持`limited`，不能表述为已获得独立热力学验证。

## 五、测试与交付状态

项目现有81项离线测试，覆盖数据模型、自然语言解析、CLI、场景路由、结果标准化、HYSYS生命周期、聊天服务、seed校验和证据采集。三个seed的SHA-256与清单一致。三个场景均有历史实机运行基线，Web目录重构后已完成HTTP健康检查。

当前系统能够完成三个固定场景从自然语言到真实HYSYS结果的闭环，但必须如实说明：现阶段打开的是预配置seed副本，并非从空白Case动态创建整套流程；反应器选择也主要是受控场景分类，还不支持考核中可能出现的任意额外场景、CSTR或PFR。下一步最有价值的改进是先实现甲苯Conversion Reactor从空白Case动态创建，再抽象公共组分、物性包、物流、Reaction Set和反应器构建器，逐步扩展至另外两个场景。

## 六、结论

本项目已经形成可重复、可审计、对输入错误采取保守策略的HYSYS自动化系统。相较于仅展示计算脚本，系统补齐了自然语言入口、工程参数验证、生命周期管理、统一结果契约和聊天交互；相较于完全通用的AI建模智能体，仍需补齐从空白Case创建和基于反应特征的通用反应器决策。当前实现适合作为三个固定考核场景的稳定演示基线，并为后续动态建模提供清晰的扩展边界。
